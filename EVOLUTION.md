# 数独游戏演进说明 (Homework 2)

本说明文档记录了从 Homework 1.1 到 Homework 2 的功能演进过程、设计决策及其背后的逻辑。

## 1. 提示功能 (Hint) 的实现
提示功能被设计为一个 **“三层协作”** 模型：
- **`Sudoku` (计算层)**：提供底层的规则计算。包括 `getCandidates`（计算指定格合法数字）和 `getNextMoves`（寻找唯余项）。它只负责“数学上的可能性”。
- **`Game` (会话层)**：负责逻辑编排。例如 `applyAnswerHint` 决定了 L3 提示必须基于 `initialGrid`（初始题面）进行求解，以保证提示的权威性，并决定操作是否存入历史栈。
- **`HintEngine` (服务层)**：负责逻辑转换。将领域层的原始计算结果（如 `[1, 5]`）包装为用户可理解的结构化数据（位置、等级、原因说明）。

### 分级策略：
- **观察级**：仅挪动光标指向“唯余”格，不填数字，不消耗次数。
- **候选+推理级**：计算当前格所有可能数字并直接填入“笔记（Notes）”中。
- **决策级**：直接从全局解中提取答案并填入。

## 2. 提示功能的归属：`Sudoku` 还是 `Game`？
我认为提示功能的**计算能力属于 `Sudoku`**，而**应用策略属于 `Game`**。
- **理由**：`Sudoku` 类拥有对棋盘阵列的直接访问权，计算候选数是纯粹的棋盘规则行为，不应依赖历史或状态。而 `Game` 类了解当前的会话上下文（如：是否为初始格、是否消耗提示次数、是否需要记录到撤销历史），它负责决定如何“消费”提示结果。这种拆分符合 **SRP（单一职责原则）**。

## 3. 探索模式 (Explore Mode) 的实现
探索模式被实现为一个显式的 **`ExploreSession`** 领域对象：
- **分叉管理**：通过 `ExploreBranch` 表达每一个平行宇宙。每个分支持有 `Game` 的一份完整 **JSON 快照 (Memento Pattern)**。
- **树状结构**：分支记录了 `parentId`。适配器层在 `sync` 时递归计算 `depth`，UI 据此实现树状缩进显示。
- **独立时间线**：通过给 `Game.undo()` 增加 `limitIndex` 参数。进入探索或切换分支时，记录当前步数为“底线”，确保用户在平行宇宙里的撤销操作不会误删主线的历史。
- **失败记忆**：利用 `Sudoku.getFingerprint()`（棋盘数字 + 笔记的组合哈希）记录失败局面。一旦用户重访已知失败的路径，系统会通过 `session.checkFailed()` 实时预警。

## 4. 主局面与探索局面的关系
当前实现采用了 **“快照隔离 (Snapshot Isolation)”** 模型：
- **非共享对象**：主局面与探索局面通过 `toJSON()` 和 `loadSnapshot()` 彻底隔离。这意味着修改一个分支的对象引用，绝不会污染主线或其他分支。
- **提交与合并**：提交（Commit）操作本质上是销毁 `ExploreSession` 管理器，保留当前活跃分支的 `Game` 实例。
- **放弃与回滚**：放弃（Cancel）操作则是将进入探索前的 `rootSnapshot` 重新加载到当前 `Game` 中。

## 5. History 结构的演进
History 结构依然保持了 **“线性操作日志”** 的简洁性，但在逻辑上演变为 **“带边界的分段线性栈”**。
- **变化点**：我们没有使用复杂的树状数据结构来存储历史，而是通过 `limitIndex`（撤销限制点）在物理线性的 `moves` 数组上划定了逻辑边界。这使得现有的 Undo/Redo 逻辑无需大规模重写即可支持分支隔离，体现了设计的**可扩展性**。

## 6. HW1 设计的局限性与反思
- **局限性 1：历史栈的不可穿透性**。HW1 的历史记录只有起点和终点，没有“分叉”的概念。在做探索模式时，发现必须给 `undo` 增加边界控制。
  - 而在本次作业中，为了支持探索模式，我们引入了“分段线性历史”的概念。
    - 技术实现：在 Game.undo 方法中增加了一个可选参数 limitIndex（撤销底线）。在主线下：limitIndex 默认为 0，可以一路退回。在分支下：每当创建一个分支（假设此时主线走了 20 步），我们就记录 branchStartIndex = 20。逻辑锁定：在这个分支里点撤销时，适配器会将 20 传给领域层。领域层执行 if (this.historyIndex > 20) 判断。
    - 带来的好处：用户在“平行宇宙”里无论怎么撤销，绝对不会误删掉主宇宙（进入探索模式前）确定的步数。
- **局限性 2：笔记（Notes）的边缘化**。HW1 中笔记只是 UI 的临时状态。在 HW2 中，为了支持分支回溯和失败记忆，必须将笔记（Notes）提升为 `Sudoku` 模型的一等公民。
  - 在这次作业中它被封装进 Sudoku 类的属性中，并进入了 Game.moves 历史记录。
    - 改动点：我们重构了 guess 接口，引入了 type: 'note-toggle'。
    - 结果：现在笔记也支持 Undo/Redo 了。在探索回溯时，笔记能随着快照自动恢复，解决了“棋盘回去了但笔记没回去”的 Bug。 
- **局限性 3：上帝对象的倾向**。原本所有的同步都在 `sync` 中完成，随着提示和探索逻辑的加入，`store.js` 变得极其臃肿。
  - 通过引入 **`ExploreSession`** 和 **`HintEngine`**，设计符合 OOD 演进：
    1.  **组合优于继承**：`Game` 组合了 `Sudoku`，而 `ExploreSession` 管理 `Game` 的快照。
    2.  **备忘录模式**：利用序列化实现历史状态的完美恢复。
    3.  **外观模式**：`gameStore` 作为统一的 Facade，屏蔽了底层复杂的平行宇宙逻辑，让 UI 开发依然保持简单。
- **局限性 4：遗留引用**。原先项目里存在“多头管理”的情况（一部分组件找旧的 `@sudoku/game`，一部分找 `gameStore`），领域对象接入是不彻底的。
  - 这次作业凡是涉及到 “改变游戏状态”（暂停、恢复、开始、落子、撤销）的操作，必须百分之百通过 gameStore。实现了对原有散乱状态管理器的完全覆盖，通过 gameStore 适配层统一了所有生命周期指令，消除了多源状态冲突的隐患。

## 7. 如果重做 HW1，我会如何修改？
如果重做，我会：
1. **尽早引入领域服务层**。不再把字符串拼接和逻辑计算写在适配器里。
2. **从第一天起就让 `Sudoku` 支持笔记**。笔记和数字本质上都是对格子的某种“占位”，应该统一处理。
3. **将 `Game` 的构造设计为完全的依赖注入**。例如 `solveSudoku` 的注入，让测试更加容易，而不是后期打补丁。


## 8. 针对评分标准的具体实现说明

### 1. 提示功能正确性
*   **分级实现**：通过 `Sudoku` 类的计算能力，实现了 **L1 (仅位置)**、**L2 (候选笔记)**、**L3 (确定答案)** 三级提示。
*   **鲁棒性**：L3 提示通过 `Game` 类调用 `solveSudoku` 始终基于 `initialGrid` 运行。这保证了即使玩家当前棋盘填得一团糟，提示系统依然能给出原始题目的正确解答，不会因逻辑冲突而崩溃。
*   **接口设计**：引入 `HintEngine` 服务，将复杂的规则扫描与自然语言生成逻辑从 UI 中解耦。

### 2. 探索模式正确性
*   **会话管理**：实现了 `ExploreSession` 类，能够完整记录探索的起点（`rootSnapshot`）。
*   **生命周期闭环**：提供了 `commitExplore`（提交：保留当前局面，销毁会话）和 `cancelExplore`（放弃：回滚至起点快照并关闭面板），确保主局面行为在退出探索后依然自洽。

### 3. 状态与对象协作设计
*   **分层协作**：
    *   **Model 层** (`Sudoku`, `Game`)：负责数据结构、撤销底线控制和指纹生成。
    *   **Session 层** (`ExploreSession`)：负责分支树的维护与失败记忆的持久化。
    *   **Adapter 层** (`gameStore`)：负责将上述复杂协作关系投影为 Svelte 响应式快照。
*   **避免退化**：探索模式的所有分支切换均通过 `loadSnapshot` 实现，保证了领域对象的完整性，而非简单的 UI 变量拼接。

### 4. History 演进合理性
*   **独立 Undo/Redo**：通过给 `Game.undo()` 注入 `limitIndex` 参数，实现了物理线性栈上的**逻辑分区**。每个分支都有自己的 `branchStartIndex`，防止了撤销操作“越界”破坏主线或其他分支的历史。
*   **防引用污染**：所有分支快照均通过 `toJSON` 序列化存储，恢复时通过工厂函数重新构造实例，彻底杜绝了 JavaScript 对象常见的深拷贝引用污染问题。

---

## 9、 核心问题回答

### Q1：你的探索模式更接近“状态切换”还是“分支会话”？

**回答：我的设计更接近“分支会话 (Branching Session)”。**

*   **理由**：
    1.  **上下文持有**：它不仅仅是一个“处于探索中”的布尔状态，它持有一个完整的 `ExploreSession` 对象，该对象管理着多个 `ExploreBranch`。
    2.  **多宇宙并行**：支持树状分支的创建与自由切换。用户可以在“分支 A”里尝试一种填法，在“分支 B”里尝试另一种，并在它们之间跳转。这已经具备了会话管理器的特征。
    3.  **独立的生命周期**：它有明确的开启（Start）、分叉（Branch）、回溯（Backtrack）和终结（Commit/Cancel）流程。
    4.  **业务记忆**：它跨越了单一状态，利用“指纹记忆”功能在整个会话期间维护一个“失败路径黑名单”。

### Q2：如果将来迁移到新的响应式机制（如 Svelte 5），你的设计中哪些部分最容易迁移，哪些最难？

**回答：**

*   **最容易迁移的部分：领域模型层 (Domain Models)**
    *   `Sudoku`、`Game`、`ExploreSession` 等类是纯粹的 **Vanilla JavaScript**。它们不依赖任何框架的 API，逻辑完全封装在类方法中。在 Svelte 5 中，这些类可以原封不动地搬过去，只需要在属性上加几个 `@state` 装饰器（或 Runes 语法）即可。
    *   `HintEngine` 服务也是纯逻辑层，迁移成本几乎为零。

*   **最难迁移的部分：适配器层 (Store Adapter - `gameStore.js`)**
    *   适配器层目前深度依赖 Svelte 3 的 `writable` store 和手动调用的 `sync()` 函数。
    *   **挑战点**：Svelte 5 引入了 `Runes` (`$state`, `$derived`)，它更倾向于**细粒度的自动追踪**，而不是我们现在这种“全量状态快照投影”。
    *   在迁移时，需要将 `sync()` 里的手动赋值重构为类的 **Getter/Setter** 自动派生逻辑。虽然这会让代码变简单，但这种从“命令式推送”到“声明式追踪”的思维转变，是适配器层重构的核心难点。

---

## 10. 加分项达成说明
1. **树状探索分支**：支持无限分叉，UI 侧边栏显示层级结构。
2. **独立 Undo / Redo**：分支操作互不干扰，撤销不越界。在探索模式中有独立的Undo和Redo，同时禁用原来的Undo和Redo按钮。
3. **提示解释能力**：`HintEngine` 生成三级不同深度的自然语言解释。
4. **状态建模**：引入了 `ExploreSession` 和 `ExploreBranch` 领域对象，而非 UI 临时变量。
5. **完整测试**：编写了 `06-hint.test.js` 和 `07-explore.test.js`，覆盖了核心逻辑。

---
